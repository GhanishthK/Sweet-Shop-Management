run = "cd backend && source venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port 8000 & cd ../frontend && npm run dev -- --host 0.0.0.0"

[nix]
channel = "stable-22_11"

[deployment]
run = ["sh", "-c", "cd backend && source venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port 8000"]
deploymentTarget = "cloudrun"